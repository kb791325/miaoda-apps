# 第二批续修方案：素材库上传收尾 + 导入导出真实化收尾 + 业务支持P0复核 + 日志分析

## 日志 LOG7680843511474670786 诊断结论（已拉取原文）
- 级别 WARN，来源 app_web（前端），触发页面：客户-公海客资，时间 2026-08-29（早于最近几次修复提交）
- 内容：`[mock] 未匹配的路径: GET entity/客户-公海客资`
- 根因：前端 request.ts 在真实后端不可用时自动降级 mock 模式，mock 路由表未注册 `entity/客户-公海客资`，旧版兜底返回假空列表冒充真实数据
- 现状：兜底已改为返回 code=503 明确错误（request.ts:1000-1005），不再假成功；该日志属历史残留。与导入导出链路的关系：公海客资是导入/导出的数据源表之一，mock 吞请求会让导入导出拿到假数据——现 entity 请求全部走真实 /api/entity，mock 路由表不含 entity/ 前缀，兜底只报错不造假，链路已闭合，无需额外修复

## 存量核实结论（上一批已提交，本批复核为主）
已完成并提交：上传控制器（upload_all≤25MB/分片三段/删除/tmp-url + ATTACH_FIELDS 自动建「素材文件/视频封面」及任务表新列）、前端 media-upload.ts（整文件/分片+进度）、素材上传弹窗动态上传区、素材页 conversion 字段统一/编辑/删除二次确认/useServerList 服务端筛选、导入 3 类型真实链路（无 setTimeout 假解析）、导出 6 可用（客户/公海客资/无效客资/线索/开户申请/财务明细）+ 3 置灰（员工/考勤/合同）+ 状态真实流转、ROI/竞品删除已接通、行业大盘无 seededRandom

## 本批剩余实施项
1. **导入补「开户申请」类型（T-02 收尾）**：entity-tables.ts:136 有真实表「广告-开户申请」，但 import-schemas.ts 只有 3 类。按其真实中文字段新增 ACCOUNT_OPEN_IMPORT_SCHEMA（枚举列校验、必填标记、模板示例行），注册进 IMPORT_SCHEMAS / IMPORT_SCHEMA_ENTRIES / IMPORT_ENTITY_APIS（tableKey→batchCreate API 映射，注意与 entity-real-columns 白名单一致）
2. **核对客户导入模板列**：与用户要求的严格列序（客户名称*,客户编号,集团名称,一级行业(枚举),二级行业,客户等级,客户状态,负责商务,所属部门,联系人,联系电话,邮箱,地址,累计充值,累计消耗,备注）逐列比对，不一致即修正
3. **全链路沙箱实测（给证据）**：
   - 上传：生成小图片 → POST /api/upload 拿 file_token → 写「业务-素材资料」记录（素材文件=[{file_token}]）→ GET 读回确认附件 url 可回显 → 清理测试记录
   - 导入：构造 3 行样例（含 1 行非法枚举）走真实写入通道验证合法行入库、非法行拦截；用 [API_TEST] 数据，测完删除
   - 导出：调 fetchAllPages 同款列表接口核对行数=真实数据行数、表头与字段一致
4. **构建与验收**：tsc/构建零错误 → 读运行时日志确认无错 → 提交 → E2E 最终验收（素材上传回显、导入四类型、导出下载、ROI/竞品删除、行业大盘三图）→ 触发发布并记录发布单号

## 不在本批（列入汇报的"仍未做"）
- 采购申请提交 500 修复（.spark 已有完整方案：JSON_TEXT_FIELDS + 服务端错误捕获，JSON_TEXT_FIELDS 尚未实施）
- 人资绩效模块联动（第一批方案已声明留第二批后）

## 风险与边界
- 「素材文件/视频封面」字段由服务端启动时 ensureField 自动建列，需实测确认建列成功；若飞书侧已存在同名列但类型不符，以 listFields 实测为准调整映射
- 分片上传（>20MB）沙箱网络受限，实测以 ≤20MB upload_all 链路为准，分片链路以代码审查+单测口径说明
- 不推倒重建、不换技术引擎、保持商务蓝/深青风格，全部为增量修补