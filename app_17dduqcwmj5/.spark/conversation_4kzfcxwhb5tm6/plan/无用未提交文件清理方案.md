## 目标与成功标准

删除真正无用的未提交文件，并让源代码管理面板/发布流程不再被「无法入库的平台文件」干扰；成功标准：`git status --short` 输出为空，发布时不再弹「当前有任务代码未提交」，且记忆功能、多维表格回写功能不受影响。

## 当前未提交清单处置（共 10 项，已核实）

| 文件 | 处置 | 理由 |
|------|------|------|
| `.spark/conversation_4kze0fv4vbrkm/`（含 2 个旧规划 md） | 删除 | 历史会话规划文档，关键信息已沉淀进 MEMORY.md（502/SERVER_HOST 条目），无运行时依赖 |
| `.agent-memory/topics/auth-env-boundaries.md` (U) | 保留+隐藏 | 被 MEMORY.md 链接引用，删除会断链 |
| `.agent-memory/MEMORY.md` (M) | 保留+隐藏 | 项目记忆主文件，不可删 |
| `server/capabilities/*_writeback_1.json` ×6 (U) | 保留+隐藏 | 在用回写插件实例的运行时配置投影（constants 直接引用其中 5 个 ID），删除 dev 会 CapabilityNotFoundError；受保护目录禁删 |

## 具体步骤

1. `rm -rf .spark/conversation_4kze0fv4vbrkm` — 删除旧会话规划目录（git status 中该 U 条目消失）
2. 编辑 `.gitignore`：在 `# Agent files` 小节（现有 `.agent/` 行旁）追加三行：`.agent-memory/`、`.spark/`、`server/capabilities/` — 隐藏 7 个 U 条目；文件仍留在磁盘，dev 运行时与平台记忆注入不受影响
3. `git update-index --skip-worktree .agent-memory/MEMORY.md` — MEMORY.md 是已跟踪文件的 M 状态，gitignore 对其无效，用 skip-worktree 隐藏（可逆：`--no-skip-worktree` 恢复）
4. 通过 commit_task 提交 `.gitignore` 改动（提交信息：忽略平台生成与记忆目录）
5. `git status --short` 验证输出为空

## 影响范围与风险

- 删除仅涉及历史规划文档，无代码/运行时引用，风险为零
- `.gitignore` 只影响 git 跟踪，不影响磁盘文件；capabilities 与记忆文件继续正常工作
- skip-worktree 仅屏蔽 MEMORY.md 的本地改动显示，平台记忆自动注入读磁盘，不受影响
- 三项操作均可逆（重新创建目录/删除 gitignore 行/no-skip-worktree）

## 验收标准

- `git status --short` 无输出
- `ls .agent-memory/topics/auth-env-boundaries.md` 与 `ls server/capabilities/` 6 个 json 均存在
- devServer 启动无错，`POST /api/bitable-retry/retry`、`repair-all` 接口可用（插件配置未被破坏）
- 用户侧发布流程不再出现未提交代码提示