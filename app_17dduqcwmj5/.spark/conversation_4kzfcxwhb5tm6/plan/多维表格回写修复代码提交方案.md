## 目标与成功标准

将上一会话遗留的 2 个业务代码文件的修改提交入库并随发布发布，确保多维表格回写的 `repair-all` 批量修复接口和 anchor 修复逻辑在生产环境生效。

## 未提交文件最终核对（共 12 项，2 项需提交）

| # | 文件路径 | 状态 | 类型 | 处理方式 |
|---|---------|------|------|---------|
| 1 | `server/modules/bitable-retry/bitable-retry.controller.ts` | M | 业务代码 | ✅ 提交 |
| 2 | `server/src/common/bitable-sync/bitable-sync.service.ts` | M | 业务代码 | ✅ 提交 |
| 3 | `.agent-memory/MEMORY.md` | M | 项目记忆 | ❌ 不提交（平台排除） |
| 4 | `.agent-memory/topics/auth-env-boundaries.md` | U | 记忆主题 | ❌ 不提交（平台排除） |
| 5 | `.spark/conversation_4kze0fv4vbrkm/plan/*.md` (2个) | U | 历史规划 | ❌ 不提交（平台排除） |
| 6 | `server/capabilities/*_writeback_1.json` (6个) | U | 插件实例配置 | ❌ 不提交（平台保护目录） |

## 改动内容（已通过 git diff 验证，完整可用）

### 1. bitable-retry.controller.ts
- 新增 `BitableRepairSummary` 类型导入
- 新增 `POST repair-all` 接口，调用 `bitableSyncService.repairAll()`

### 2. bitable-sync.service.ts
- 新增 `isNull` 导入、`BitableRepairSummary` 类型导入
- **修复 anchor 逻辑**：回写 record id 从仅读 `bitableRecordId` 改为 `bitableRecordId ?? baseRecordId`（解决同步表无 bitableRecordId 时回写失败的问题）
- **新增 `repairAll()` 方法**：扫描 9 张表（student/schedule/attendance/course/faq/material/equipment/processFlow/formula）中 `bitableRecordId` 为空的记录，逐条调用 `syncRecord` 批量回写，返回 `{total, synced, failed, skipped}` 汇总

### 完整性验证
- ✅ `shared/bitable-sync.ts` 已有 `BitableRepairSummary` 类型定义（第28行）
- ✅ `server/app.module.ts` 已注册 `BitableRetryModule`（第11行import，第25行imports）
- ✅ 各业务模块（course/consultation/content/schedule/student）均已注入 `BitableSyncService`
- ✅ `bitable-retry.module.ts` 已注册 `BitableSyncService` 和 `BitableSyncMapper`

## 具体步骤

1. 通过 `commit_task` 提交 2 个业务文件的改动，提交信息："新增多维表格批量回写修复接口并修复anchor逻辑"
2. 提交完成后，点击发布弹窗中的「提交并发布」或「继续发布」均可（业务代码已入库则不影响）

## 影响范围与风险

- **影响功能**：多维表格回写能力（新增批量修复入口 + 修复同步表回写锚点）
- **风险等级**：低
  - 新增接口 `repair-all` 是新增端点，不影响现有逻辑
  - anchor 逻辑从 `bitableRecordId` 改为 `bitableRecordId ?? baseRecordId`，扩大了回写可命中的范围，对已有 `bitableRecordId` 的记录行为不变
- **注意**：`repair-all` 为同步执行，9 张表全量扫描+逐条回写可能耗时较长（数据量大时可能超时），仅作为运维工具手动触发，不建议前端频繁调用

## 验收标准

- 代码提交后 `git status` 中 2 个业务文件不再显示为已修改
- 发布后调用 `POST /api/bitable-retry/repair-all` 返回 200 且含 `{total, synced, failed, skipped}` 字段
- 已有回写功能（单条 retry）行为不变
- 同步表中 `baseRecordId` 有值但 `bitableRecordId` 为空的记录，回写时能正确命中 baseRecordId 对应记录进行更新