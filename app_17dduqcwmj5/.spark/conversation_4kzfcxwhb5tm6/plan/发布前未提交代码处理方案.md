## 目标与成功标准

用户在点击发布时遇到「当前有任务代码未提交」弹窗，需判断 10 项未提交文件是否影响发布、是否需要提交，并给出明确处理方式。

## 未提交文件清单与性质判定

| # | 文件路径 | 类型 | 是否影响业务发布 | 处理方式 |
|---|---------|------|-----------------|---------|
| 1 | `.agent-memory/MEMORY.md` (M) | 项目记忆文件 | 否 | 不提交 |
| 2 | `.agent-memory/topics/auth-env-boundaries.md` (U) | 记忆主题文件 | 否 | 不提交 |
| 3 | `.spark/conversation_4kze0fv4vbrkm/plan/502网关问题修复方案.md` (U) | 历史规划文档 | 否 | 不提交 |
| 4 | `.spark/conversation_4kze0fv4vbrkm/plan/线上环境设置SERVER_HOST.md` (U) | 历史规划文档 | 否 | 不提交 |
| 5 | `server/capabilities/content_material_library_writeback_1.json` (U) | 回写插件实例配置 | 否 | 不提交（受保护目录） |
| 6 | `server/capabilities/course_sheet_writeback_1.json` (U) | 回写插件实例配置 | 否 | 不提交（受保护目录） |
| 7 | `server/capabilities/equipment_tool_table_writeback_1.json` (U) | 回写插件实例配置 | 否 | 不提交（受保护目录） |
| 8 | `server/capabilities/faq_knowledge_base_writeback_1.json` (U) | 回写插件实例配置 | 否 | 不提交（受保护目录） |
| 9 | `server/capabilities/process_flow_table_writeback_1.json` (U) | 回写插件实例配置 | 否 | 不提交（受保护目录） |
| 10 | `server/capabilities/recipe_detail_table_writeback_1.json` (U) | 回写插件实例配置 | 否 | 不提交（受保护目录） |

## 关键事实依据

1. `.agent-memory/`：平台明确排除，不进入应用代码库，提交也不会随发布生效
2. `.spark/conversation_*/`：会话级规划文档，平台排除目录，不参与构建发布
3. `server/capabilities/`：根据 MEMORY.md 已记录的经验，此目录为平台保护/自动生成目录，bash/write 均禁改，发布由平台侧实例配置兜底，不依赖本地 JSON 文件

## 处理方案

推荐操作：直接点击「继续发布」（即忽略这些未提交文件）。

理由：10 项文件全部为非业务代码（记忆/规划文档 + 平台受保护的插件实例配置），不提交不影响应用功能和发布成功率。

## 验收标准

- 点击「继续发布」后发布流程正常推进
- 发布后应用功能（课程/学员/排期/考勤/内容中心/咨询FAQ）均正常，无功能缺失
- 多维表格回写能力正常（平台侧实例配置兜底，与本地 capabilities JSON 无关）

## 补充说明

若后续确实需要将记忆文件或插件配置纳入版本管理（非发布必需），可单独提交 `.agent-memory/` 目录，但不会改变发布产物。`server/capabilities/` 目录不应手动提交，由平台插件实例管理系统维护。